export interface Tablet {
  nome: string;
  sku: string;
  descricao: string
}

export interface Desktop {
  nome: string;
  sku: string;
  descricao: string;
}

export interface Wifi {
  nome: string;
  sku: string;
  descricao: string;
}
